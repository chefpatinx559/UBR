{
  "name": "ubr-frontend",
  "version": "1.0.0",
  "description": "Frontend pour UBR - Union Bénie et Réelle",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "backend": "cd ubr-backend && php artisan serve --host=0.0.0.0 --port=8000",
    "start": "concurrently \"npm run backend\" \"npm run dev\""
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}